import {describe,expect,it} from "vitest";import {summarizeDiagnostic} from "./diagnostic";import {buildDevelopmentPlan} from "./development-plan";
describe("four-week plan",()=>{it("transforme les priorités en progression explicable",()=>{const diagnostic=summarizeDiagnostic({availability:3,scanning:2,progression:4,reactionAfterLoss:1});const plan=buildDevelopmentPlan(diagnostic);expect(plan.weeks).toHaveLength(4);expect(plan.primaryTheme).toBe("Récupérer rapidement");expect(plan.weeks.map((week)=>week.phase)).toEqual(["Découvrir","Stabiliser","Mettre sous pression","Évaluer"]);expect(plan.explanation).toContain("1/4");});});
