import{render,screen}from"@testing-library/react";import{describe,expect,it}from"vitest";import PlanPage from"./page";describe("development plan",()=>{it("présente quatre semaines et leur raison",()=>{render(<PlanPage/>);expect(screen.getAllByText(/^S[1-4]$/)).toHaveLength(4);expect(screen.getByText(/comportement le plus fragile/i)).toBeVisible();expect(screen.getByRole("button",{name:/adopter ce cycle/i})).toBeVisible();});});
